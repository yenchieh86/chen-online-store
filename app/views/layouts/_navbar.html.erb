<div class='container-fluid'>
    <div class='navbar_area'>
        <div class='row'>
            <div class='col-md-1'>
                <ul class='nav nav-pills'>
                    <%= render 'layouts/navbar_form', object: Category %>
                </ul>
            </div>
            <% if current_user %>
                <div class='col-md-2 checkout_btn text-right'>
                    <%= link_to 'Check Out', new_user_charge_path(current_user), class: 'btn btn-default' %>
                </div>
            <% end %>
        </div>
    </div>
</div>

