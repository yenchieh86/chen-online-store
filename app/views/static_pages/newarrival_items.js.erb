$('#new_arrival_item_list').append('<%= j render 'items/item_list', items: @items %>')
<% if @items.next_page %>
  $('.pagination').replaceWith('<%= j paginate @items %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>

